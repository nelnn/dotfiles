#!/bin/sh


# Initial setup for a Proxmox CT
# At least for Debian 13


apt-get update
apt install -y curl wget git rsync sudo tmux

# docker
curl -fsSL https://get.docker.com | sh

# tailscale
# https://tailscale.com/docs/features/containers/lxc/lxc-unprivileged
# Put /dev/net/tun in Resources -> Add -> Device Passthrough
curl -fsSL https://tailscale.com/install.sh | sh
tailscale up -ssh
tailscale set --auto-update
# tailscale serve --bg <port>


